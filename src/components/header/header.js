import React from 'react';
import './header.css';

const Header = () => {
  return (
    <div>
      <nav className="sticky-wrapper" id="sticky-wrapper"> 
        <a href="#">Link</a>
      </nav>
    </div>
  )
}

export default Header;